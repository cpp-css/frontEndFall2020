import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    avatar: {
        width: 200,
    },
    row: {
        marginTop: 20,
    },
    imageContainer: {
        alignItems: 'center',
        height: 100,
        marginTop: '10%',
        marginBottom: '10%'
    },
    image: {
        height: '100%',
        width: '100%'
    }
});

export default styles;